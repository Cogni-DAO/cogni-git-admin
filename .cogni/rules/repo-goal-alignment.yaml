id: cogni-git-review-repo-goal-alignment
schema_version: "0.3"
blocking: true
workflow_id: goal-evaluations

# Edit this! Update it with a clear description of the repo's goal.
evaluations:
  - cogni-admin-goal: This PR directly advances the development of building a mature Github Admin bot, for setting up and controlling github repos from a remote onchain DAO activity.
  - admin-only: This PR accrues to Admin app functionality, and does not introduce bot functionality that does not require admin permissions. 
  # - clear-sub-goals: This PR only uses open source tools and does not introduce new paid dependencies.



# Code-aware capabilities (vendor-prefixed for non-breaking compatibility)
x_capabilities:
  - diff_summary      # Enable enhanced diff summary with file stats
  - file_patches      # Include actual patch content for analysis

success_criteria:
  neutral_on_missing_metrics: true
  require:
    - metric: cogni-admin-goal
      gte: 0.8
    - metric: admin-only
      gte: 0.8